name: aiact-rag
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - requests>=2.31
      - beautifulsoup4>=4.12
      - lxml>=5.0
      - langchain-text-splitters>=0.3
      - tiktoken>=0.7
      - pydantic>=2.6
      - python-dotenv>=1.0
      - pytest>=8.0
      # future deps (langchain, qdrant-client, mistralai, ragas, mlflow, fastapi, presidio)
      # not installed yet
