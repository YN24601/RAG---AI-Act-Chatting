name: aiact-rag
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - requests>=2.31
      - beautifulsoup4>=4.12
      - lxml>=5.0
      - langchain-text-splitters>=0.3
      - tiktoken>=0.7
      - pydantic>=2.6
      - python-dotenv>=1.0
      - pytest>=8.0
      # --- Day 3-4: embedding, indexing & retrieval ---
      - langchain-mistralai>=0.2
      - langchain-qdrant>=0.2
      - qdrant-client>=1.12
      # --- Day 5: orchestration (LangGraph) & tracing (LangSmith) ---
      - langgraph>=0.2
      - langsmith>=0.1
      # later: cohere (Day 3-4 rerank, deferred), ragas, mlflow, fastapi, presidio
